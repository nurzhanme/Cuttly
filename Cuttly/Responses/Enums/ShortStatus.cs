namespace Cuttly.Responses.Enums
{
    public enum ShortStatus
    {
        LINK_ALREADY_SHORTENED = 1,
        IS_NOT_LINK,
        NAME_TAKEN,
        INVALID_API_KEY,
        CONTAINS_INVALID_CHARACTERS,
        BLOCKED_DOMAIN,
        OK
    }
}